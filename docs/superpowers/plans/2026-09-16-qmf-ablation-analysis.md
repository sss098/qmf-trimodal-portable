# QMF 消融与预测诊断实施计划

目标：保留原始分组和 formal_v1，完成患者级统计诊断，提供两种排序消融的可运行指令；不启动正式训练。

- [x] 为排序模式和零系数写回归测试：原仓库 MarginRankingLoss 数值/梯度一致，默认结果不变，零系数等于四项分类监督。
- [x] config/cli/losses/training 增加 rank_mode=positive_margin/original 及命令行覆盖；保存到既有配置契约。
- [x] reports/qmf_diagnostics.py 读取已保存折外预测，输出折内/种子内/分类别相关性、尺度分布、平移敏感性、患者配对分层 Bootstrap 95% CI。
- [x] 测试统计函数的已知值、配对一致性、常数相关性和 q 平移性质，运行完整单测与格式检查。
- [x] 文档去掉标签来源及诊断讨论，新增简洁结果报告及消融说明/指令；正式结果文件校验保持不变。

约束：三个网络、融合公式、数据处理和五折名单不变；每组3种子×5折；最大100轮、早停12、选轮后重训；原排序保留零分母保护。已有正间隔 QMF 作基线，仅新增无排序及原排序训练。代码变化后不能直接恢复旧代码指纹的 formal_v1；新实验采用独立名称。
