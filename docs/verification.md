# 验证记录

历史验证日期：2026-09-15。详细证据见verification.json。原verification02_smoke大产物已于2026-09-16清理，其轻量记录保留在archives/old_smoke_metadata.tar.gz；当前整理验证见下文及cleanup.md。

- Ruff静态检查通过；Ruff格式检查通过。
- 21项测试通过：特征与原程序数值对照、FTTransformer参数与梯度、QMF质量梯度与排序、DS显式公式与梯度、TMC损失、患者拆分、训练集预处理、缓存代码指纹、UTF-8和配置约束。
- 三方法均完成RTX3060 Laptop GPU上的真实数据seed42/fold0验证：51人选择训练1轮、17人验证；重新初始化后68人重训1轮、17人测试。默认batch6、每人40个EMG训练窗通过。
- 每个模型首批检查所有可训练参数均得到梯度，全部训练步检查非有限损失/梯度。
- CPU上的真实训练器中断恢复测试：以轻量注入模型隔离训练器行为，三方法恢复参数与连续训练逐项完全一致；没有声称完整模型CUDA中断恢复逐位一致。
- 已完成同配置GPU短程任务的`--resume`重入检查，完整输出未重复训练。
- 独立代码审查发现的缓存版本绑定、100轮硬上限均已修正并加回归测试。中文编码环境问题已复现和修正。
- 历史短测完成后，formal_v1的3方法×3种子×5折正式实验已全部完成。正式指标见reports/formal_v1_analysis.md；短测指标不是研究性能结论。

## 2026-09-16整理验证

全项目自有代码Ruff检查通过；src/tests/reports格式检查通过；23项测试通过。新增归档对照证明EMG前向、参数梯度及标量特征与formal_v1原代码数值完全一致。报告脚本重新计算得到与归档JSON相同的统计结果。QMF排序测试只验证本版排序行为，不代表与原仓库rank_loss数值等价。旧短测大文件清理后，历史验证记录及原始代码仍保留在归档和本文中，详见cleanup.md。
