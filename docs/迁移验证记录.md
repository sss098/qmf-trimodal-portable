# 独立目录验证记录

验证对象为本目录，而非原项目。主线模型为 `noise_aug_v1`，重新训练写入 `portable_noise_aug_v1`。

- 663个输入资产和105个正式最终模型的校验通过。
- 85名患者、标签、五折名单、95项候选字段与原参考记录一致。
- 源码、配置和运行脚本没有原机器 `/home/wp24` 的硬编码依赖。
- 目录中没有软链接。
- 实际将整个目录改名到含空格的路径，从 `/tmp` 启动数据检查与GPU扰动评估，通过无扰动预测一致性检查。
- 从打包的原始文件重新生成肌电窗口与表格缓存，并对等权融合、QMF各完成一轮选轮和重训，成功保存模型并评估。
- Python测试50项通过；Ruff静态检查、格式检查及全部Shell入口语法检查通过。
- 完整汇报中的公式分隔符、代码块配对及Markdown本地文件链接检查通过。
- 旧smoke结果及本次验证产生的smoke结果均已删除，正式模型和分析文件保留。

验证使用原机器现有兼容Python环境。另一台机器上的全新依赖安装、GPU驱动兼容性仍需按README执行环境检查；Python环境不随数据目录直接复制。
